# RabbitMQ
pika>=1.3.2

# Data reading
pandas>=2.2.0
openpyxl>=3.1.0       # required by pandas for .xlsx

# Validation & config
pydantic>=2.7.0
pydantic-settings>=2.2.0

# Database registry (SQLite by default, swap for PostgreSQL in prod)
sqlalchemy>=2.0.0

# HTTP API (optional — only needed if you run uvicorn api.routes:app)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
